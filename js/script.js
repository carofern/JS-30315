// Crear un algoritmo con un condicional

let askAge = prompt('Ingresa tu edad:');

if (askAge >= 18) {
    console.log('Tienes permitido entrar al Club');
} else {
    console.log('No puedes ingresar al Club, eres menor de edad');
}

// console.log(askAge); no porque imprime la edad